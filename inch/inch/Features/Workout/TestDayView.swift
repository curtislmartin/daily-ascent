import SwiftUI
import SwiftData

/// Placeholder — implemented in Phase 6 (Workout Session).
struct TestDayView: View {
    let enrolmentId: PersistentIdentifier

    var body: some View {
        Text("Test Day")
            .navigationTitle("Test Day")
    }
}
